<Window 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MigrationWizard 2.x"
        Height="260" Width="700"
        WindowStartupLocation="CenterScreen">
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Ligne 1 : chemin du dossier -->
    <TextBlock Grid.Row="0"
               Text="Dossier d'export / import du profil :"
               FontWeight="Bold"
               Margin="0,0,0,5"/>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <TextBox Name="tbProfileFolder"
               Grid.Column="0"
               Margin="0,0,5,0"/>

      <Button Name="btnBrowse"
              Grid.Column="1"
              Content="Parcourir..."
              Width="90"/>
    </Grid>

    <!-- Ligne 2 : options -->
    <GroupBox Grid.Row="2"
              Header="Éléments à inclure"
              Margin="0,8,0,8">
      <UniformGrid Columns="3" Margin="5">
        <CheckBox Name="cbAll" Content="Tout cocher / décocher"/>

        <CheckBox Name="cbWifi"           Content="Profils Wi-Fi"/>
        <CheckBox Name="cbPrinters"       Content="Imprimantes"/>
        <CheckBox Name="cbNetworkDrives"  Content="Lecteurs réseau"/>
        <CheckBox Name="cbRdp"            Content="Connexions RDP"/>
        <CheckBox Name="cbBrowsers"       Content="Navigateurs (AppData)"/>
        <CheckBox Name="cbOutlook"        Content="Outlook (AppData)"/>
        <CheckBox Name="cbWallpaper"      Content="Fond d'écran"/>
        <CheckBox Name="cbDesktopLayout"  Content="Disposition du bureau"/>
        <CheckBox Name="cbTaskbarStart"   Content="Barre des tâches / menu Démarrer"/>
      </UniformGrid>
    </GroupBox>

    <!-- Ligne 3 : boutons -->
    <StackPanel Grid.Row="3"
                Orientation="Horizontal"
                HorizontalAlignment="Center"
                Margin="0,0,0,5">
      <Button Name="btnExportProfile"
              Content="Exporter le profil"
              Width="140"
              Margin="5,0"/>
      <Button Name="btnImportProfile"
              Content="Importer le profil"
              Width="140"
              Margin="5,0"/>
    </StackPanel>

    <!-- Ligne 4 : statut -->
    <TextBlock Grid.Row="4"
               Name="lblStatus"
               Text="Prêt."
               Margin="0,0,0,0"/>
  </Grid>
</Window>
